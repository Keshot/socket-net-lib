{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_client",
            "type": "shell",
            "command": "sh ${workspaceFolder:test_client}/build_client.sh"
        }
    ]
}